#include <stdio.h>
#include <math.h>

int main() {
    float length, breadth, side, base, height, radius;
    float areaRectangle, areaSquare, areaTriangle, areaCircle;

    
    printf("Enter length and breadth of rectangle: ");
    scanf("%f %f", &length, &breadth);
    areaRectangle = length * breadth;

    
    printf("Enter side of square: ");
    scanf("%f", &side);
    areaSquare = side * side;

    
    printf("Enter base and height of triangle: ");
    scanf("%f %f", &base, &height);
    areaTriangle = 0.5 * base * height;

    
    printf("Enter radius of circle: ");
    scanf("%f", &radius);
    areaCircle = M_PI * radius * radius; 

   
    printf("\nArea of Rectangle = %.2f", areaRectangle);
    printf("\nArea of Square = %.2f", areaSquare);
    printf("\nArea of Triangle = %.2f", areaTriangle);
    printf("\nArea of Circle = %.2f\n", areaCircle);

    return 0;
}